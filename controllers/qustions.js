const getAllQustion = (req, res) => {
  res.send('getAllQustion')
};

const getQustion = (req, res) => {
  res.send('getQustion')
};

const addQustion = (req, res) => {
  res.send('addQustion')
};

const editQustion = (req, res) => {
  res.send('editQustion')
};

const deleteQustion = (req, res) => {
  res.send('deleteQustion')
};

module.exports = {
  getAllQustion,
  getQustion,
  addQustion,
  editQustion,
  deleteQustion
};